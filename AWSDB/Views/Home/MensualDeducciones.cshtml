@model CombinedViewModel
<form asp-controller="MensualDeducciones" method="post">
    <div>
        <label class="Titulo-IndexU">Deducciones Mensual</label>
    </div>
   
    <div class="row">
        <div class="table-responsive">
            <table class="table table-striped" style="overflow-y:auto;">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Porcentaje</th>
                        <th>Monto de Deduccion</th>
                    </tr>
                </thead>
                <tbody>
                    @*@foreach (var item in Model.LeadDetails)
                    {
                        <tr>
                            <td>@item.Codigo</td>
                            <td>@item.Nombre</td>
                            <td>@item.ClaseArticulo</td>
                           
                        </tr>
                    }*@
                </tbody>
                <tbody>

					<div class="input-group-append">
                        <button asp-action="" type="submit" class="btn btn-primary">Regresar</button>
					</div>

				</tbody>
			</table>
		</div>
	</div>
    <div class="form-group" style="display:none;">
        <label asp-for="UserName">@Model.UserName</label>
        <input asp-for="UserName" class="form-control" />
    </div>
</form>
@if (TempData["Message"] != null) 
{
	<script>
		alert('@TempData["Message"]');
	</script>
}